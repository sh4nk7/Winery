/**
 * Course is an Activity subclass which is in charge of saving a course with its properties.
 */
public class Course extends Activity {

    public Course(final String n, final Person[] pa) {
        //child class of 'Activity'
        super(n, pa);
    }
}
