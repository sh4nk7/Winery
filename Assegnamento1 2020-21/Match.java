package com.company;

/**
 * Match is an Activity subclass which is in charge of saving a match with its properties.
 */
public class Match extends Activity {
    public Match(final String n, final Person[] pa) {
        //child class of 'Activity'
        super(n, pa);
    }
}
